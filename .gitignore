.site/
node_modules
node_modules/
.DS_Store
.codex/config.toml
.claude/skills/sync-docs
.claude/settings.local.json

# Screenshot pipeline — ephemeral seed state and scratch outputs
scripts/screenshots/.seed-ids.json

# Local scratch area (probe scripts, throwaway captures)
scratchpad/

# Internal evidence ledgers are delivered as private task artifacts, not in the repo
.artifacts/
